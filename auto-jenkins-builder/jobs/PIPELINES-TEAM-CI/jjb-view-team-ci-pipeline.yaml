### VIEWS ###
- view:
    name: jenkins-builders-team-ci
    view-type: list
    description: 'Jenkins View for TEAM CI'
    filter-executors: false
    filter-queue: false
    job-name:
        - jjb-dat-ci-pipeline
        - jjb-et1-ci-pipeline
        - jjb-et2-ci-pipeline
    columns:
        - status
        - weather
        - job
        - last-success
        - last-failure
        - last-duration
        - last-duration
        - build-button

### Notice. Add a new job-name on the first place of list.
### If it doesn't help. Put not added  job at the first place and update VIEW again.

